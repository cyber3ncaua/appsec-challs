sudo: false

language: node_js
node_js:
  - node
 
install:
  - yarn install
  
script:
  - yarn test
  - yarn run build 
  
deploy:
  provide: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: main
  
notification:
  email: false
